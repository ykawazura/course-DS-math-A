\input{preamble}
\begin{document}
\begin{tikzpicture}
  % Axes
  \draw[axis] (-3.5,0) -- (3.5,0) node[below] {$x$};
  \draw[axis] (0,-2) -- (0,6) node[left] {$y$};

  % 1/x^2 curve (left branch)
  \draw[very thick, domain=-3:-0.42, samples=50] plot (\x, {1/(\x*\x)});

  % 1/x^2 curve (right branch)
  \draw[very thick, domain=0.42:3, samples=50] plot (\x, {1/(\x*\x)});

  % Red arrows approaching from left
  \draw[red, very thick, -{Stealth[length=6pt]}, domain=-1.2:-0.48, samples=30]
    plot (\x, {1/(\x*\x)});

  % Red arrows approaching from right
  \draw[red, very thick, -{Stealth[length=6pt]}, domain=1.2:0.48, samples=30]
    plot (\x, {1/(\x*\x)});
\end{tikzpicture}
\end{document}
